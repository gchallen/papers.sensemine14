#!/usr/bin/env python

import argparse

from power.lib import * #@UnusedWildImport
from statistics.lib import * #@UnusedWildImport

parser = argparse.ArgumentParser()
args = parser.parse_args()


s = Statistic.load(verbose=True)
collected, total = s.log_coverage()

print """We estimated that we collected %.0f\\%% of the logs generated by phones during our experiment.""" % (collected * 100.0 / total,) 